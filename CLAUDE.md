# CLAUDE.md

## 项目定位

本项目是“绿电直连风光储多方案批量测算软件”。

当前阶段只开发 V0.1 技术测算模块，不开发经济性评价模块。

## 必读文档

在修改代码前，必须阅读：

- PRD.md
- ALGORITHM_SPEC.md
- DATA_SCHEMA.md
- OUTPUT_SCHEMA.md
- TEST_CASES.md
- UI_SPEC.md
- ECONOMY_EXTENSION_SPEC.md

## 核心原则

1. 算法正确性优先于界面美观。
2. 不允许擅自修改 ALGORITHM_SPEC.md 中的逐时能量平衡逻辑。
3. 不允许引入电网给储能充电。
4. 不允许储能同时充电和放电。
5. 不允许忽略 SOC 上下限。
6. 不允许忽略储能功率约束。
7. 不允许忽略储能容量约束。
8. 不允许把储能损耗错误计入弃电。
9. 不允许把经济性评价混入 V0.1。
10. 所有核心计算函数必须可测试。
11. 每次修改核心算法后必须运行 pytest。
12. 输出必须包含方案汇总和逐小时明细。
13. 支持 8760 和 8784 小时数据。
14. 负荷使用真实值，风电和光伏使用标幺曲线。
15. 储能 SOC 必须逐小时滚动。

## 技术栈

- Python
- pandas
- numpy
- pydantic
- pytest
- openpyxl
- xlsxwriter
- streamlit
- pyyaml

## 项目结构要求

建议结构：

```text
src/green_direct/
├─ io/
├─ models/
├─ core/
├─ batch/
├─ export/
├─ economy/
├─ ui/
└─ cli.py
```

## 开发规则

- 每次只完成用户指定阶段。
- 不要一次性重写整个项目。
- 不要删除测试。
- 不要删除文档。
- 修改算法前必须说明原因。
- 如果发现需求矛盾，先在代码注释或回复中说明，不要自行猜测。
- 如果测试失败，应先修复测试失败，再开发新功能。

## 完成标准

一个阶段完成必须满足：

1. 代码能运行；
2. 测试通过；
3. 文档没有被破坏；
4. 没有引入当前阶段不要求的功能；
5. 说明修改了哪些文件。
