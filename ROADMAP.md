# ROADMAP.md：开发路线图

状态：2026-05-19 架构重定向版

本项目已从“V0.1 风光储批量技术测算工具”调整为“绿电直连 / 微电网项目方案策划与推荐平台”。旧 V0.1 仍作为技术基线保留，但未来路线不再以“枚举结果表”为最终产品形态。

## Baseline：V0.1 技术测算基线

已实现或已形成基线：

- 数据导入；
- 数据校验；
- 8760 / 8784 小时支持；
- 风光储单方案逐时平衡；
- 储能 SOC 滚动；
- 风光负值按站用电处理；
- 多方案批量测算；
- 政策指标校核；
- 电网交换功率限制；
- 结果导出；
- Streamlit 初步界面；
- 图表模块初步能力。

定位：

- 继续作为回归测试和计算口径基线；
- 不再作为最终产品边界；
- 后续新增功能不得无意破坏该基线。

## V0.2：架构重定向与推荐骨架

目标：

- 建立 `ProjectStudy` / `StudyResult` 上层概念；
- 建立结构化输入诊断；
- 建立 `RecommendationPortfolio` 推荐结果模型；
- 在不重写核心算法的情况下包装现有 `run_batch`；
- 主输出从“全量枚举表”转向“代表方案组合”。

新增：

- 输入审查：阻断、警告、提示；
- 推荐方案标签；
- 推荐理由；
- 代表方案去重；
- UI 初步改为围绕推荐方案展示。

## V0.3：轻量经济性排序

目标：

- 在技术仿真结果基础上增加经济性排序；
- 支持方案推荐时按经济性筛选；
- 不做完整财务模型，不改变技术调度。

初期指标：

- 总投资估算；
- 年运维费用；
- 下网购电成本；
- 上网收入；
- 年净成本；
- LCOE-like 筛选指标；
- 简单回收期，若参数足够。

后续预留：

- NPV；
- IRR；
- 多年现金流；
- 税费；
- 折旧；
- 融资。

## V0.4：柴发与离网 / 备用场景

目标：

- 加入柴油发电资产；
- 支持离网风光储柴场景；
- 支持并网加柴发备用场景；
- 增加供电可靠性指标。

新增：

- 柴发容量；
- 柴发出力；
- 柴油消耗；
- 柴油成本；
- 柴油排放；
- 缺供电量；
- 缺供小时；
- 最大缺供功率。

初期调度建议：

- 离网：新能源 -> 储能 -> 柴发 -> 缺供；
- 并网加备用：新能源 -> 储能 -> 电网 -> 柴发兜底。

待专家确认：

- 柴发是否允许给储能充电；
- 是否需要最小稳定出力；
- 是否需要启停约束；
- 柴发与电网同时存在时是否允许经济性调度。

## V0.5：大批量结果存储与按需明细

目标：

- 解决大量方案枚举时的内存和 UI 性能问题；
- summary 常驻，逐小时明细按需生成或持久化；
- 推荐方案优先加载明细。

新增：

- `ResultStore`；
- `outputs/runs/{study_id}/` 运行结果目录；
- 按需加载单方案 hourly ledger；
- 推荐方案导出包。

## V0.6：报告与专家审查包

目标：

- 围绕推荐方案生成更完整的项目测算材料；
- 支持人工复核和专家审查。

新增：

- 项目输入摘要；
- 输入诊断报告；
- 推荐方案对比；
- 技术指标说明；
- 经济性排序说明；
- 典型日 / 年度时序图；
- 能量流向图；
- 逐小时明细附件；
- 关键口径说明。

说明：

- 当前已有图表模块只是早期原型，效果不理想；
- 图表能力不作为近期架构核心；
- 等逐时计算、规模配置、经济性排序和推荐逻辑稳定后，再重新设计图表和报告。

## V1.0：完整方案策划平台

目标：

- 多项目管理；
- 参数方案保存；
- 完整经济性评价；
- 完整报告生成；
- 更成熟的 UI；
- 可选安装包或桌面启动器。

可能新增：

- NPV / IRR / 投资回收期；
- 同一主体和不同主体投资关系；
- 分时电价；
- 柴发经济调度；
- 储能来源分账；
- 碳排评价；
- 敏感性分析。

## V1.1+：高级优化

可能方向：

- MILP 优化接口；
- 多年资源情景；
- 典型日聚类；
- 分时电价下储能优化调度；
- 可靠性约束优化；
- 多目标 Pareto 前沿；
- 更复杂的柴发启停模型。
