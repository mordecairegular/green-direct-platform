# DATA_SCHEMA.md：输入数据格式说明

## 1. 文件类型

V0.1 默认支持 CSV 文件。

后续可扩展支持 Excel。

## 2. 编码

软件应自动尝试以下编码：

1. UTF-8；
2. UTF-8-SIG；
3. GBK；
4. GB18030。

如果全部失败，应提示用户转换文件编码。

## 3. 时间序列长度

支持：

- 8760 行：普通年；
- 8784 行：闰年。

三条曲线必须行数一致，时间戳必须一致。

## 4. 负荷曲线文件

### 4.1 推荐字段

```csv
时间,负荷
2020-01-01 00:00:00,10.25
2020-01-01 01:00:00,10.10
```

### 4.2 单位

负荷单位：万千瓦。

### 4.3 校验规则

- 时间列必须能解析为 datetime；
- 负荷值不能为空；
- 负荷值不能小于 0；
- 负荷列可通过用户界面选择；
- 如果没有标准字段名，应允许用户指定列。

## 5. 光伏曲线文件

### 5.1 推荐字段

```csv
时间,光伏
2020-01-01 00:00:00,0
2020-01-01 01:00:00,0
```

### 5.2 单位

光伏为标幺值，无单位。

### 5.3 校验规则

- 时间列必须能解析为 datetime；
- 光伏值不能为空；
- 微小负值可按配置截断为 0；
- 明显负值应报错；
- 大于 1 的值默认允许但提示；
- 是否将大于 1 的值截断为 1，由参数控制。

## 6. 风电曲线文件

### 6.1 推荐字段

```csv
时间,风电
2020-01-01 00:00:00,0.32
2020-01-01 01:00:00,0.35
```

### 6.2 单位

风电为标幺值，无单位。

### 6.3 校验规则

同光伏曲线。

## 7. 标幺曲线处理

如果用户上传的是原始出力曲线，而不是标幺曲线，应允许用户输入基准装机容量：

```text
pv_pu = pv_raw / pv_base_capacity
wind_pu = wind_raw / wind_base_capacity
```

V0.1 可先默认用户上传的是标幺曲线，但代码结构要预留转换函数。

## 8. 时间对齐

三条曲线合并前必须校验：

```text
load.time == pv.time == wind.time
```

如果不一致，禁止计算，并显示不一致的样例行。

## 9. 数据清洗默认参数

```yaml
allow_small_negative_pu: true
small_negative_tolerance: -0.001
clip_small_negative_to_zero: true
allow_pu_greater_than_one: true
clip_pu_greater_than_one: false
```

## 10. 数据单位

| 数据 | 单位 |
|---|---|
| 负荷功率 | 万千瓦 |
| 光伏容量 | 万千瓦 |
| 风电容量 | 万千瓦 |
| 储能功率 | 万千瓦 |
| 储能容量 | 万千瓦时 |
| 每小时电量 | 万千瓦时 |
| SOC | 0-1 |
