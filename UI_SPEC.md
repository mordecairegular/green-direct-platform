# UI_SPEC.md：初步交互界面说明

## 1. 技术路线

V0.1 使用 Streamlit 构建初步交互界面。

原因：

- 开发快；
- 适合数据上传和结果展示；
- 易于后续打包；
- 对非专业编程用户友好。

## 2. 页面结构

### 2.1 页面标题

```text
绿电直连风光储多方案批量测算工具 V0.1
```

### 2.2 左侧参数栏

分为以下折叠区：

1. 数据上传；
2. 容量搜索范围；
3. 储能参数；
4. 上网与政策约束；
5. 输出设置；
6. 高级参数。

### 2.3 主页面

显示：

1. 数据校验结果；
2. 方案数量预估；
3. 开始测算按钮；
4. 方案汇总表；
5. 达标方案表；
6. 不达标原因统计；
7. 下载按钮；
8. 单方案逐小时明细查看。

## 3. 数据上传区域

用户上传：

- 负荷 CSV；
- 光伏 CSV；
- 风电 CSV。

支持用户选择：

- 时间列；
- 负荷列；
- 光伏列；
- 风电列。

## 4. 容量搜索范围

建议输入方式：

```text
起始值、结束值、步长
```

例如：

| 参数 | 默认值 |
|---|---|
| 光伏容量起始 | 0 |
| 光伏容量结束 | 30 |
| 光伏容量步长 | 5 |
| 风电容量起始 | 0 |
| 风电容量结束 | 30 |
| 风电容量步长 | 5 |
| 储能功率起始 | 0 |
| 储能功率结束 | 10 |
| 储能功率步长 | 2 |
| 储能时长选项 | 0, 2, 4 |

## 5. 储能参数

默认值：

| 参数 | 默认值 |
|---|---:|
| 初始 SOC | 0.5 |
| 最小 SOC | 0.1 |
| 最大 SOC | 0.9 |
| 充电效率 | 0.95 |
| 放电效率 | 0.95 |
| 循环寿命 | 6000 |

## 6. 政策参数

默认值：

| 参数 | 默认值 |
|---|---:|
| 自发自用率下限 | 0.60 |
| 绿电占用电比例下限 | 0.30 |
| 上网比例上限 | 0.20 |
| 是否允许上网 | 是 |
| 最大上网功率 | 不限制或用户输入 |

## 7. 结果展示

### 7.1 汇总指标卡片

显示：

- 总方案数；
- 达标方案数；
- 最高清洁电力占比；
- 最低弃电率；
- 最低上网比例；
- 最大储能循环次数。

### 7.2 方案表格

用 `st.dataframe` 显示方案汇总表。

### 7.3 筛选控件

支持：

- 只看达标方案；
- 按绿电占比排序；
- 按弃电率排序；
- 按上网比例排序；
- 按储能容量筛选。

### 7.4 下载按钮

提供：

- 下载方案汇总 Excel；
- 下载全部逐小时明细 ZIP；
- 下载当前方案逐小时 CSV。

## 8. 错误提示

必须对用户友好，不能只显示 Python 报错。

示例：

```text
三条曲线的时间列不一致，请检查第 125 行附近的时间戳。
```

```text
光伏曲线存在明显负值，小于允许阈值 -0.001，请检查数据。
```

```text
本次配置将生成 12000 个方案，可能计算较慢，建议增大步长或缩小范围。
```

## 9. 性能限制提醒

V0.1 建议方案数超过 5000 时给出提醒。

## 10. 初版界面验收

- 能上传 CSV；
- 能设置参数；
- 能运行批量测算；
- 能显示方案汇总；
- 能下载结果；
- 出错时有明确提示。
